function soma(a: number, b: number): number {
    return a + b;
}
console.log(soma(5, 10));

function calcularTotal(preco: number, quantidade: number): number {
    return preco * quantidade;
}

console.log(calcularTotal(10, 8));